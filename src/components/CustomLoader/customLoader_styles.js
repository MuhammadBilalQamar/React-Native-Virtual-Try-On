import { StyleSheet } from "react-native";

// these are the styles for custom loader component
export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "row",
  },
});
